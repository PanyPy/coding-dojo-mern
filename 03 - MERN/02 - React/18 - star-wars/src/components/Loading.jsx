import React from "react";
import logo from '../logo.svg';

export default (
    <header className="App-header">
    <img src={logo} className="App-logo" alt="logo" />
    <p>Loading... May the force be with you!</p>
  </header>   
);