prependVal(ListNode,val,before) insert a new ListNode with the value `val` before `before` node
appendVal(list, val, after) -> insert the val after `after` node
removeVal(ListNode, val) -> remove the node with the given value